spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:15432/tinkoff-hw-db
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver
  data:
    mongodb:
      uri: mongodb://localhost:27017/tinkoff-hw-mongodb
    redis:
      host: localhost
      port: 6379
      password: YXNkZmFzZGY=
  cache:
    type: redis
    cache-names: redis-cache
    redis:
      time-to-live: 30000
  liquibase:
    change-log: classpath:/db/changelog/db-changelog.xml
    enabled: true
    drop-first: false
    default-schema: public
  graphql:
    graphiql:
      enabled: true
    path: /graphql

minio:
  url: http://localhost:9000
  port: 9000
  access-key: user
  secret-key: password
  secure: false
  bucket: minio-storage
  image-size: 10485760

server:
  port: 8080

logging:
  level:
    "liquibase": info

